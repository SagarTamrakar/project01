<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{viewTitle}}</title>
</head>
<body>
    <form action="/api/login" method="POST" autocomplete="off">
    <div>
    <div class="form-group">
        <label>Email:</label>
        <input type="text" class="form-control" name="email" placeholder="Email" value="{{user.email}}">
        <div class="text-danger">
            {{user.emailError}}
        </div>
    </div>    
    <div class="form-group">
        <label>Password:</label>
        <input type="number" class="form-control" name="password" placeholder="PIN" value="{{user.password}}">
        <div class="text-danger">
            {{user.passwordError}}
        </div>    
    </div>
    </div>
         <div>
        <button type="submit" class="btn btn-info"><i class="fa fa-database"></i> Submit</button>
        <a class="btn btn-secondary" href="/api/users" ><i class="fa fa-list-alt"></i> Create new user</a>

    </div>
</form>
</body>
</html>