<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome To Hind Enterprises </title>
</head>
<body>
<form action="/api/users" method="POST" autocomplete="off">
<div class="form-group">
        <label>Username:</label>
        <input type="text" class="form-control" name="userName" placeholder="Type Username..." value="{{user.userName}}">
        <div class="text-danger">
            {{user.userNameError}}
        </div>
    </div>
    <div>
    <div class="form-group">
        <label>Email:</label>
        <input type="text" class="form-control" name="email" placeholder="Email" value="{{user.email}}">
        <div class="text-danger">
            {{user.emailError}}
        </div>
    </div>    
    <div class="form-group">
        <label>Password:</label>
        <input type="number" class="form-control" name="password" placeholder="Password" value="{{user.password}}">
        <div class="text-danger">
            {{user.passwordError}}
        </div>    
    </div>
    </div>
      <div class="form-row">
        <div class="from-group col-md-6">
           <label>Mobile:</label>
           <input type="text" class="form-control" name="mobile" placeholder="Mobile" value="{{user.mobile}}">
           <div class="text-danger">
            {{user.mobileError}}
            </div>
        </div>
        <div class="form-group col-md-6">
            <label>isAdmin:</label>
            <input type="text" class="form-control" name="isAdmin" placeholder="True/False" value="{{user.isAdmin}}">
            <div class="text-danger">
            {{user.isAdmin}}
            </div>
        </div>
    </div>  
    <div>
        <button type="submit" class="btn btn-info"><i class="fa fa-database"></i> Submit</button>
        <a class="btn btn-secondary" href="/api/login" ><i class="fa fa-list-alt"></i> Already registered</a>

    </div>
</form>
</body>
</html>